---
phase: 11-end-to-end-verification-type-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Discovery plugin uses SDK client.agents.list() and client.health()"
    - "Language model streaming uses SDK client.agents.runStream()"
    - "Language model non-streaming uses SDK client.agents.run()"
    - "Continue uses SDK client.agents.continue()"
    - "Cancel uses SDK client.agents.cancel()"
    - "Processor reads pausedState from providerMetadata and passes to LLM.continueAgentOS()"
    - "No custom Zod schemas exist in agentos provider directory"
    - "No hand-written AgentOS API response types exist (only app-specific state types and SDK re-exports)"
    - "TypeScript compiles cleanly"
  artifacts:
    - path: "packages/opencode/src/plugin/agentos.ts"
      provides: "Agent discovery via SDK"
      contains: "client.agents.list"
    - path: "packages/opencode/src/provider/sdk/agentos/agentos-language-model.ts"
      provides: "Streaming and lifecycle via SDK"
      contains: "client.agents.runStream"
    - path: "packages/opencode/src/provider/sdk/agentos/agentos-client.ts"
      provides: "SDK client singleton"
      contains: "AgentOSClient"
    - path: "packages/opencode/src/session/processor.ts"
      provides: "Tool confirmation workflow"
      contains: "agentOSPausedState"
    - path: "packages/opencode/src/session/llm.ts"
      provides: "LLM continue bridge"
      contains: "continueAgentOS"
  key_links:
    - from: "packages/opencode/src/plugin/agentos.ts"
      to: "packages/opencode/src/provider/sdk/agentos/agentos-client.ts"
      via: "getAgentOSClient()"
      pattern: "getAgentOSClient"
    - from: "packages/opencode/src/provider/sdk/agentos/agentos-language-model.ts"
      to: "@worksofadam/agentos-sdk"
      via: "config.getClient() then client.agents.runStream/run/continue/cancel"
      pattern: "client\\.agents\\."
    - from: "packages/opencode/src/session/processor.ts"
      to: "packages/opencode/src/session/llm.ts"
      via: "LLM.continueAgentOS()"
      pattern: "LLM\\.continueAgentOS"
    - from: "packages/opencode/src/session/llm.ts"
      to: "packages/opencode/src/provider/sdk/agentos/agentos-language-model.ts"
      via: "language.continueRun()"
      pattern: "language\\.continueRun"
---

<objective>
Verify the full agent chat workflow end-to-end (discover -> connect -> stream -> tool confirm -> continue/cancel) and confirm all v2.0 type requirements are satisfied.

Purpose: This is the final verification for the v2.0 SDK migration. Proves requirements TYPE-01, TYPE-02, TYPE-03, and RUN-03 are met by inspecting wiring chains across the codebase.

Output: Verification results confirming all requirements satisfied, ready for phase verification.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-end-to-end-verification-type-cleanup/11-01-SUMMARY.md
@packages/opencode/src/plugin/agentos.ts
@packages/opencode/src/provider/sdk/agentos/agentos-language-model.ts
@packages/opencode/src/provider/sdk/agentos/agentos-client.ts
@packages/opencode/src/provider/sdk/agentos/agentos-types.ts
@packages/opencode/src/provider/sdk/agentos/agentos-provider.ts
@packages/opencode/src/provider/sdk/agentos/index.ts
@packages/opencode/src/session/processor.ts
@packages/opencode/src/session/llm.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify full workflow wiring chain (RUN-03)</name>
  <files></files>
  <action>
Run grep-based verification to prove the complete agent chat workflow is wired end-to-end through the SDK. This is READ-ONLY verification -- no files are modified.

**Chain 1: Discovery (plugin -> SDK)**
1. Verify `packages/opencode/src/plugin/agentos.ts` calls `getAgentOSClient()` to get SDK client
2. Verify it calls `client.health()` for health check
3. Verify it calls `client.agents.list()` for agent discovery
4. Verify it uses `AgentResponse` type from SDK re-export
5. Verify it handles SDK error types (`APIError`, `AuthenticationError`)

**Chain 2: Streaming (language model -> SDK)**
1. Verify `agentos-language-model.ts` imports SDK types (`AgentOSClient`, `AgentStream`, `StreamEvent`, etc.)
2. Verify `doStream()` calls `client.agents.runStream()` for streaming
3. Verify `doGenerate()` calls `client.agents.run()` for non-streaming
4. Verify event handlers process: RunStarted, RunContent, ToolCallStarted, ToolCallCompleted, RunCompleted, RunPaused, RunError
5. Verify RunPaused builds `AgentOSPausedState` and passes via `providerMetadata`

**Chain 3: Tool Confirmation (processor -> llm -> language model -> SDK)**
1. Verify `processor.ts` imports `AgentOSPausedState` type
2. Verify `processor.ts` reads `value.providerMetadata?.agentos?.pausedState` in finish-step handler
3. Verify `processor.ts` iterates `agentOSPausedState.requirements` and calls `PermissionNext.ask()`
4. Verify `processor.ts` calls `LLM.continueAgentOS()` with the paused state
5. Verify `llm.ts` `continueAgentOS()` gets `AgentOSLanguageModel` and calls `language.continueRun()`
6. Verify `agentos-language-model.ts` `continueRun()` calls `client.agents.continue()` with tools JSON
7. Verify `agentos-language-model.ts` `cancelRun()` calls `client.agents.cancel()`

**Chain 4: Abort Signal (abort -> cancel)**
1. Verify `doStream()` wires `options.abortSignal` to `cancelRun()` in RunStarted handler
2. Verify abort listener is cleaned up after stream completes

For each verification point, run a grep command and record the file:line evidence. If any chain link is broken, report it as a gap.
  </action>
  <verify>
All grep commands return matches confirming each chain link exists. No broken links found.
  </verify>
  <done>Complete wiring chain verified: discovery (plugin -> SDK list), streaming (language model -> SDK runStream/run), tool confirmation (processor -> llm -> language model -> SDK continue), cancellation (abort signal -> SDK cancel). RUN-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Verify type requirements (TYPE-01, TYPE-02, TYPE-03) and compile</name>
  <files></files>
  <action>
Run verification checks to prove all three type requirements are met. This is READ-ONLY verification -- no files are modified.

**TYPE-01: Custom Zod schemas removed**
1. Run `grep -r "z\.object\|z\.string\|z\.number\|z\.boolean\|z\.array\|z\.enum\|z\.union\|from.*zod" packages/opencode/src/provider/sdk/agentos/` -- should return NO matches
2. Run `grep -r "ZodSchema\|ZodType\|ZodObject" packages/opencode/src/provider/sdk/agentos/` -- should return NO matches
3. Confirm: All Zod validation removed, SDK handles validation internally

**TYPE-02: SDK types re-exported where needed**
1. Verify `agentos-types.ts` exports `AgentResponse` (SDK re-export, used by plugin)
2. Verify `agentos-types.ts` exports `ModelResponse` (SDK re-export, available for consumers)
3. Verify `index.ts` barrel exports both SDK re-exports AND app-specific types
4. Verify `plugin/agentos.ts` imports `AgentResponse` from the types file
5. Verify `session/processor.ts` imports `AgentOSPausedState` from the types file
6. Verify `session/llm.ts` imports `AgentOSPausedState` from the types file
7. Verify `agentos-language-model.ts` imports `AgentOSPausedState` and `AgentOSRequirement` from types file

**TYPE-03: No hand-written AgentOS API types remain**
1. Run `grep -r "interface AgentOS" packages/opencode/src/provider/sdk/agentos/agentos-types.ts` and classify each:
   - `AgentOSToolExecution` -- APP-SPECIFIC (has `confirmed`, `confirmation_note` not in SDK) -- ACCEPTABLE
   - `AgentOSRequirement` -- APP-SPECIFIC (wraps tool execution with confirmation state) -- ACCEPTABLE
   - `AgentOSPausedState` -- APP-SPECIFIC (groups run/session/agent/requirements) -- ACCEPTABLE
   - `AgentOSProviderSettings` -- FACTORY CONFIG (only `name?: string` after cleanup) -- ACCEPTABLE
2. Verify NO types exist that mirror SDK API response types (like the old `AgentOSAgent`, `AgentOSModelInfo`, `AgentOSBaseEvent`, etc.)
3. Run `grep -r "AgentOSAgent\|AgentOSModelInfo\|AgentOSBaseEvent\|AgentOSRunResponse\|AgentOSSSE" packages/opencode/src/` -- should return NO matches (only comment references allowed)

**Final compilation check:**
Run `cd packages/opencode && bunx tsc --noEmit` -- must exit 0 with no errors.

Record all evidence and report pass/fail for each requirement.
  </action>
  <verify>
`cd packages/opencode && bunx tsc --noEmit` exits 0.
All TYPE-01, TYPE-02, TYPE-03 grep checks pass.
No Zod schemas, no hand-written API types, SDK types properly re-exported.
  </verify>
  <done>TYPE-01 satisfied (zero Zod schemas). TYPE-02 satisfied (AgentResponse, ModelResponse re-exported; all app-specific types accessible via barrel). TYPE-03 satisfied (only app-specific state types and SDK re-exports remain, no hand-written API response types). TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. All 4 wiring chains verified with grep evidence (discovery, streaming, tool confirmation, abort/cancel)
2. `grep -r "z\.\|from.*zod" packages/opencode/src/provider/sdk/agentos/` returns 0 matches (TYPE-01)
3. All 6 types exported from barrel index.ts (TYPE-02)
4. `grep -r "AgentOSAgent\|AgentOSModelInfo\|AgentOSBaseEvent" packages/opencode/src/` returns 0 matches (TYPE-03)
5. `cd packages/opencode && bunx tsc --noEmit` exits 0
6. Requirements RUN-03, TYPE-01, TYPE-02, TYPE-03 all satisfied
</verification>

<success_criteria>
- All 4 wiring chains have verified grep evidence at every link
- Zero Zod schemas in agentos provider directory
- SDK types properly re-exported and imported by consumers
- No hand-written API response types remain (only app-specific state)
- TypeScript compilation succeeds
- All 4 Phase 11 requirements (TYPE-01, TYPE-02, TYPE-03, RUN-03) confirmed satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-end-to-end-verification-type-cleanup/11-02-SUMMARY.md`
</output>
