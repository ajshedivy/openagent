---
phase: 14-user-facing-text-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/index.ts
  - packages/opencode/src/cli/cmd/serve.ts
  - packages/opencode/src/cli/cmd/uninstall.ts
  - packages/opencode/src/cli/cmd/pr.ts
  - packages/opencode/src/cli/cmd/run.ts
  - packages/opencode/src/cli/cmd/auth.ts
  - packages/opencode/src/cli/cmd/web.ts
  - packages/opencode/src/cli/cmd/upgrade.ts
  - packages/opencode/src/cli/cmd/github.ts
  - packages/opencode/src/cli/cmd/import.ts
  - packages/opencode/src/cli/cmd/mcp.ts
  - packages/opencode/src/cli/cmd/tui/attach.ts
  - packages/opencode/src/cli/cmd/tui/thread.ts
autonomous: true

must_haves:
  truths:
    - "User sees 'openagent' in help text and --version output"
    - "User sees 'openagent' in error messages"
    - "User sees 'openagent' in server startup messages"
    - "User sees 'openagent' in command descriptions"
    - "User cannot see 'opencode' in CLI output during normal usage"
  artifacts:
    - path: "packages/opencode/src/index.ts"
      provides: "scriptName('openagent') and log reference to openagent"
      min_lines: 160
    - path: "packages/opencode/src/cli/cmd/serve.ts"
      provides: "Server startup message with 'openagent server'"
      min_lines: 20
    - path: "packages/opencode/src/cli/cmd/uninstall.ts"
      provides: "Uninstall prompts referencing 'openagent'"
      min_lines: 200
    - path: "packages/opencode/src/cli/cmd/web.ts"
      provides: "Web command description with 'openagent'"
      min_lines: 60
    - path: "packages/opencode/src/cli/cmd/mcp.ts"
      provides: "MCP command instructions with 'openagent'"
      min_lines: 700
  key_links:
    - from: "packages/opencode/src/index.ts"
      to: "yargs scriptName"
      via: ".scriptName() method call"
      pattern: "\\.scriptName\\([\"']openagent[\"']\\)"
    - from: "packages/opencode/src/cli/cmd/serve.ts"
      to: "console.log"
      via: "server startup message"
      pattern: "openagent server listening"
---

<objective>
Replace all user-facing "opencode" text strings with "openagent" to complete branding transition.

Purpose: Users see consistent "openagent" branding in CLI help, error messages, logs, and prompts — no "opencode" references in normal usage.

Output: User-visible text fully branded as "openagent" across help text, command descriptions, error messages, log output, and interactive prompts.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI script name and core messaging</name>
  <files>packages/opencode/src/index.ts</files>
  <action>
Replace user-facing "opencode" strings with "openagent":

1. Line 44: Change `.scriptName("opencode")` to `.scriptName("openagent")`
   - This controls what appears in help text (e.g., "openagent [command]")

2. Line 73: Change `Log.Default.info("opencode", {...})` to `Log.Default.info("openagent", {...})`
   - Internal log messages should reference openagent for consistency

Do NOT change:
- Line 71: `process.env.OPENCODE = "1"` — keep for backward compatibility
- Import statements from `@opencode-ai/*` packages — these are npm package names, not user-facing
  </action>
  <verify>grep 'scriptName("openagent")' packages/opencode/src/index.ts && grep 'Log.Default.info("openagent"' packages/opencode/src/index.ts</verify>
  <done>Help text shows "openagent" as command name, log messages reference "openagent"</done>
</task>

<task type="auto">
  <name>Task 2: Update command descriptions and console output</name>
  <files>
    packages/opencode/src/cli/cmd/serve.ts
    packages/opencode/src/cli/cmd/uninstall.ts
    packages/opencode/src/cli/cmd/pr.ts
    packages/opencode/src/cli/cmd/run.ts
    packages/opencode/src/cli/cmd/auth.ts
    packages/opencode/src/cli/cmd/web.ts
    packages/opencode/src/cli/cmd/upgrade.ts
    packages/opencode/src/cli/cmd/github.ts
    packages/opencode/src/cli/cmd/import.ts
    packages/opencode/src/cli/cmd/mcp.ts
    packages/opencode/src/cli/cmd/tui/attach.ts
    packages/opencode/src/cli/cmd/tui/thread.ts
  </files>
  <action>
Update user-facing text in command files:

**serve.ts:**
- Line 9: Change `describe: "starts a headless opencode server"` to `"starts a headless openagent server"`
- Line 16: Change `console.log(\`opencode server listening...\`)` to `\`openagent server listening...\``

**uninstall.ts:**
- Line 57: Change `prompts.intro("Uninstall OpenCode")` to `prompts.intro("Uninstall openagent")`
- Line 234: Change `prompts.log.success("Thank you for using OpenCode!")` to `"Thank you for using openagent!"`

**pr.ts:**
- Line 8: Change `describe: "fetch and checkout a GitHub PR branch, then run opencode"` to `"...then run openagent"`
- Line 71: Message `"Found opencode session:"` to `"Found openagent session:"`
- Line 91: Message `"Starting opencode..."` to `"Starting openagent..."`
- Line 97: Keep `spawn("opencode", ...)` — this references the actual binary name which is already "openagent" via bin mapping (no change needed, but verify comment below)
- Line 105: Message `"opencode exited with code"` to `"openagent exited with code"`

**run.ts:**
- Line 30: Change `describe: "run opencode with a message"` to `"run openagent with a message"`
- Line 84: Change `describe: "attach to a running opencode server..."` to `"...openagent server..."`

**auth.ts:**
- Line 222: Change `describe: "opencode auth provider"` to `"openagent auth provider"`
- Line 333: Message referencing `opencode.json` should stay as-is (config file name, not branding)
- Line 342: Message referencing `opencode.json` should stay as-is (config file name, not branding)
- Line 347-348: `if (provider === "opencode")` logic — keep condition unchanged (this checks for legacy opencode.ai provider, not our app name)

**web.ts:**
- Line 34: Change `describe: "start opencode server and open web interface"` to `"start openagent server and open web interface"`

**upgrade.ts:**
- Line 8: Change `describe: "upgrade opencode to the latest or a specific version"` to `"upgrade openagent to the latest or a specific version"`
- Line 30: Change `prompts.log.error(\`opencode is installed to ${process.execPath}...\`)` to `\`openagent is installed to ${process.execPath}...\``

**github.ts:**
- Line 536: Change `console.log("opencode session", session.id)` to `console.log("openagent session", session.id)`
- Line 881: Change `console.log("Sending message to opencode...")` to `console.log("Sending message to openagent...")`

**import.ts:**
- Line 14: Keep `describe: "path to JSON file or opencode.ai share URL"` unchanged — this references the domain name opencode.ai (external service), NOT our app name

**mcp.ts:**
- Line 87: Change `prompts.outro("Add servers with: opencode mcp add")` to `prompts.outro("Add servers with: openagent mcp add")`
- Line 164: Keep first part of message but clarify: `prompts.log.info("Remote MCP servers support OAuth by default. Add a remote server in opencode.json:")` — "opencode.json" is a config filename (stays per Phase 13), so no change needed

**tui/attach.ts:**
- Line 6: Change `describe: "attach to a running opencode server"` to `"attach to a running openagent server"`

**tui/thread.ts:**
- Line 45: Change `describe: "start opencode tui"` to `"start openagent tui"`
- Line 50: Change `describe: "path to start opencode in"` to `"path to start openagent in"`

Do NOT change:
- Import statements from `@opencode-ai/*` packages
- References to `opencode.json` config file (Phase 13 established dual support)
- `OPENCODE_*` environment variable names (backward compatibility decision)
- `spawn("opencode", ...)` in pr.ts — binary is already mapped to openagent via package.json bin field
- References to "opencode.ai" domain/URLs — these are external service references
- `provider === "opencode"` conditions — legacy provider checks
  </action>
  <verify>
cd packages/opencode && \
grep -n "describe.*openagent" src/cli/cmd/serve.ts src/cli/cmd/run.ts src/cli/cmd/auth.ts src/cli/cmd/pr.ts src/cli/cmd/web.ts src/cli/cmd/upgrade.ts src/cli/cmd/tui/attach.ts src/cli/cmd/tui/thread.ts && \
grep -n "openagent server listening" src/cli/cmd/serve.ts && \
grep -n "Uninstall openagent" src/cli/cmd/uninstall.ts && \
grep -n "openagent mcp add" src/cli/cmd/mcp.ts && \
grep -n "openagent session" src/cli/cmd/github.ts && \
grep -n "Sending message to openagent" src/cli/cmd/github.ts
  </verify>
  <done>
All command descriptions show "openagent", console output messages reference "openagent", interactive prompts display "openagent"
  </done>
</task>

</tasks>

<verification>
**Manual verification steps:**

1. Run help command:
   ```bash
   cd packages/opencode
   bun run src/index.ts --help
   # Should show: "openagent [command]" at top
   ```

2. Check version output:
   ```bash
   bun run src/index.ts --version
   # Should work without showing "opencode" anywhere
   ```

3. Check command descriptions:
   ```bash
   bun run src/index.ts serve --help
   # Should show: "starts a headless openagent server"

   bun run src/index.ts run --help
   # Should show: "run openagent with a message"

   bun run src/index.ts web --help
   # Should show: "start openagent server and open web interface"

   bun run src/index.ts upgrade --help
   # Should show: "upgrade openagent to the latest or a specific version"
   ```

4. Grep check for remaining user-facing "opencode":
   ```bash
   cd packages/opencode/src
   grep -r '"opencode"' cli/cmd/*.ts index.ts | grep -v "@opencode-ai" | grep -v "opencode.json" | grep -v "opencode.ai" | grep -v "OPENCODE_" | grep -v "provider === \"opencode\""
   # Should return minimal results (only backward-compat refs and config file names)
   ```
</verification>

<success_criteria>
- [ ] `openagent --help` shows "openagent [command]" not "opencode"
- [ ] Command descriptions (serve, run, pr, auth, web, upgrade, mcp, attach, tui) reference "openagent"
- [ ] Server startup message says "openagent server listening"
- [ ] Uninstall prompts say "Uninstall openagent"
- [ ] Error messages and console output use "openagent"
- [ ] MCP help text says "openagent mcp add"
- [ ] GitHub command console logs reference "openagent session"
- [ ] No user-visible "opencode" text in normal CLI usage
- [ ] Config file names (opencode.json/openagent.json) unchanged (Phase 13)
- [ ] Environment variable names (OPENCODE_*) unchanged (backward compat)
- [ ] Domain references (opencode.ai) unchanged (external service)
</success_criteria>

<output>
After completion, create `.planning/phases/14-user-facing-text-branding/14-01-SUMMARY.md`
</output>
