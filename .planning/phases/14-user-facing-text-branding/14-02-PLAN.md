---
phase: 14-user-facing-text-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/cli/cmd/tui/component/tips.tsx
  - packages/opencode/src/cli/cmd/tui/app.tsx
  - packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TUI tips display 'openagent' branding, not 'OpenCode'"
    - "Update notifications reference 'openagent upgrade', not 'opencode upgrade'"
    - "Provider dialogs reference 'openagent Zen', not 'OpenCode Zen'"
    - "User cannot see 'opencode' or 'OpenCode' in TUI tips and notifications"
  artifacts:
    - path: "packages/opencode/src/cli/cmd/tui/component/tips.tsx"
      provides: "TUI tips with openagent branding"
      contains: "openagent"
    - path: "packages/opencode/src/cli/cmd/tui/app.tsx"
      provides: "Update notification with openagent branding"
      contains: "openagent v"
    - path: "packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx"
      provides: "Provider dialog with openagent Zen branding"
      contains: "openagent Zen"
  key_links:
    - from: "packages/opencode/src/cli/cmd/tui/component/tips.tsx"
      to: "user screen"
      via: "TUI tips display"
      pattern: "openagent"
    - from: "packages/opencode/src/cli/cmd/tui/app.tsx"
      to: "user notifications"
      via: "toast.show message"
      pattern: "openagent v.*openagent upgrade"
---

<objective>
Complete Phase 14 user-facing text branding by updating remaining TUI components (tips, notifications, provider dialogs) to reference "openagent" instead of "OpenCode"/"opencode".

Purpose: Close verification gaps identified in Phase 14 verification — eliminate all remaining user-visible "opencode"/"OpenCode" references in TUI tips, update notifications, and provider dialogs.

Output: Three files updated with consistent "openagent" branding throughout TUI user-facing text.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-user-facing-text-branding/14-01-SUMMARY.md
@.planning/phases/14-user-facing-text-branding/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TUI tips branding</name>
  <files>packages/opencode/src/cli/cmd/tui/component/tips.tsx</files>
  <action>
Replace all user-visible "OpenCode" and "opencode" command references with "openagent" in the TIPS array. Per Phase 14 branding decision, use lowercase "openagent" consistently.

**OpenCode → openagent replacements (brand name in tips):**
- Line 90: "OpenCode auto-handles OAuth" → "openagent auto-handles OAuth"
- Line 99: "OpenCode auto-formats files" → "openagent auto-formats files"
- Line 102: "OpenCode uses LSP servers" → "openagent uses LSP servers"
- Line 107: "prevent OpenCode from reading" → "prevent openagent from reading"
- Line 112: "for headless API access to OpenCode" → "for headless API access to openagent"
- Line 146: "Use /connect with OpenCode Zen" → "Use /connect with openagent Zen"

**opencode → openagent command references (CLI commands in tips):**
- Line 83: "Create {highlight}opencode.json{/highlight}" → "Create {highlight}openagent.json{/highlight}"
- Line 84: "~/.config/opencode/opencode.json" → "~/.config/openagent/openagent.json"
- Line 91: ".opencode/command/" → ".openagent/command/"
- Line 94: ".opencode/agent/" → ".openagent/agent/"
- Line 103: ".opencode/tool/" → ".openagent/tool/"
- Line 105: ".opencode/plugin/" → ".openagent/plugin/"
- Line 108: "opencode run" → "openagent run"
- Line 109: "opencode --continue" → "openagent --continue"
- Line 110: "opencode run -f file.ts" → "openagent run -f file.ts"
- Line 112: "opencode serve" → "openagent serve"
- Line 113: "opencode run --attach" → "openagent run --attach"
- Line 114: "opencode upgrade" → "openagent upgrade"
- Line 115: "opencode auth list" → "openagent auth list"
- Line 116: "opencode agent create" → "openagent agent create"
- Line 117: "/opencode in GitHub" → "/openagent in GitHub"
- Line 118: "opencode github install" → "openagent github install"
- Line 119: "/opencode fix this" → "/openagent fix this"
- Line 122: ".opencode/themes/" → ".openagent/themes/"
- Line 138: "opencode debug config" → "openagent debug config"
- Line 145: "docker run -it --rm ghcr.io/anomalyco/opencode" → "docker run -it --rm ghcr.io/anomalyco/openagent"

**Config file references:** Note that opencode.json tips (lines 83-84) should be updated to openagent.json as the new preferred config file name per Phase 13 dual support. However, opencode.json still works as a fallback.

Preserve existing {highlight} markup and tip structure.
  </action>
  <verify>
Run grep to confirm no "OpenCode" or "opencode" command references remain in user-visible tip strings:
```bash
grep -n "OpenCode\|opencode" packages/opencode/src/cli/cmd/tui/component/tips.tsx | grep -v "import\|opencode.ai"
```

Expected: No matches (except opencode.ai domain reference which is intentional).
  </verify>
  <done>
- TIPS array contains "openagent" brand name (not "OpenCode")
- TIPS array references "openagent" commands (not "opencode")
- Config file tips reference "openagent.json" as preferred name
- Directory tips reference ".openagent/" paths
- External domain "opencode.ai" preserved (not user-facing app text)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TUI update notification and warning</name>
  <files>packages/opencode/src/cli/cmd/tui/app.tsx</files>
  <action>
Update two user-visible text references in app.tsx to use "openagent" branding consistently:

**Update notification (line 668):**
- Change: `OpenCode v${evt.properties.version} is available. Run 'opencode upgrade' to update manually.`
- To: `openagent v${evt.properties.version} is available. Run 'openagent upgrade' to update manually.`

**OpenRouter warning (line 606):**
- Change: "For reliable access to models check out OpenCode Zen"
- To: "For reliable access to models check out openagent Zen"

Both changes use lowercase "openagent" per Phase 14 branding consistency decision.
  </action>
  <verify>
Grep for remaining "OpenCode" references in app.tsx user messages:
```bash
grep -n "OpenCode" packages/opencode/src/cli/cmd/tui/app.tsx
```

Expected: No matches (terminal title already updated in 14-01).
  </verify>
  <done>
- Update notification shows "openagent v{VERSION}" and suggests "openagent upgrade"
- OpenRouter warning references "openagent Zen"
- No user-visible "OpenCode" text remains in app.tsx
  </done>
</task>

<task type="auto">
  <name>Task 3: Update provider dialog branding</name>
  <files>packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx</files>
  <action>
Update the "opencode" provider dialog description (line 233) to use "openagent Zen" branding:

**Change:**
- Line 233: "OpenCode Zen gives you access to all the best coding models..."

**To:**
- Line 233: "openagent Zen gives you access to all the best coding models..."

Use lowercase "openagent" per Phase 14 branding consistency. The service is now called "openagent Zen" (not "OpenCode Zen").
  </action>
  <verify>
Grep for "OpenCode" in dialog-provider.tsx:
```bash
grep -n "OpenCode" packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx
```

Expected: No matches.
  </verify>
  <done>
- Provider dialog description shows "openagent Zen" (not "OpenCode Zen")
- No user-visible "OpenCode" text remains in dialog-provider.tsx
  </done>
</task>

</tasks>

<verification>
**Post-execution checks:**

1. **Grep check for remaining user-visible OpenCode/opencode references:**
   ```bash
   # Check TUI components for remaining brand references (excluding internal identifiers)
   grep -r "OpenCode\|opencode" packages/opencode/src/cli/cmd/tui/ | grep -v "opencode.ai" | grep -v "provider.id.*opencode" | grep -v "theme.*opencode" | grep -v "import"
   ```
   Expected: Only internal identifiers, external domains (opencode.ai), and import paths — no user-visible text.

2. **Verify tips content:**
   Spot-check 3-4 updated tip strings to confirm "openagent" branding:
   ```bash
   grep -A 1 "auto-handles OAuth\|auto-formats files\|openagent run" packages/opencode/src/cli/cmd/tui/component/tips.tsx
   ```

3. **Verify update notification:**
   ```bash
   grep "Update Available" -A 2 packages/opencode/src/cli/cmd/tui/app.tsx
   ```
   Expected: Message shows "openagent v{VERSION}" and "openagent upgrade".

4. **Verify provider dialog:**
   ```bash
   grep -B 2 -A 2 "openagent Zen" packages/opencode/src/cli/cmd/tui/component/dialog-provider.tsx
   ```
   Expected: Dialog shows "openagent Zen gives you access...".

5. **Phase 14 verification re-run:**
   After gap closure, Phase 14 verification should pass all 7 truths.
</verification>

<success_criteria>
1. All TUI tips reference "openagent" (not "OpenCode" or "opencode commands")
2. Update notification shows "openagent v{VERSION}" and suggests "openagent upgrade"
3. Provider dialogs reference "openagent Zen" consistently
4. Config file tips recommend "openagent.json" as primary config file
5. Directory tips reference ".openagent/" paths
6. External domain "opencode.ai" preserved (not app branding)
7. Grep verification confirms no user-visible "OpenCode"/"opencode" references remain
8. Phase 14 goal achieved: User cannot see "opencode" in normal TUI usage
</success_criteria>

<output>
After completion, create `.planning/phases/14-user-facing-text-branding/14-02-SUMMARY.md`
</output>
