---
phase: 12-package-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/package.json
autonomous: true

must_haves:
  truths:
    - "package.json name field is @worksofadam/openagent"
    - "package.json version field is 0.1.0"
    - "package.json description references openagent and AgentOS"
    - "package.json bin field maps openagent to ./bin/openagent"
    - "package.json author field identifies Adam Shedivy"
    - "package.json repository points to github.com/ajshedivy/openagent"
  artifacts:
    - path: "packages/opencode/package.json"
      provides: "Scoped npm package identity for @worksofadam/openagent"
      contains: "@worksofadam/openagent"
  key_links:
    - from: "packages/opencode/package.json"
      to: "packages/opencode/bin/openagent"
      via: "bin field mapping"
      pattern: "\"openagent\".*\"./bin/openagent\""
---

<objective>
Update package.json metadata to establish the `@worksofadam/openagent` package identity.

Purpose: Package identity fields (name, version, description, author, repository, keywords, homepage) must reflect the openagent brand and scoped npm publishing name before any downstream phases (config, branding, publishing) can proceed.

Output: Updated `packages/opencode/package.json` with correct package identity.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/opencode/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json identity fields</name>
  <files>packages/opencode/package.json</files>
  <action>
Update the following fields in `packages/opencode/package.json`:

1. **name**: Change from `"opencode"` to `"@worksofadam/openagent"`
2. **version**: Change from `"1.1.42"` to `"0.1.0"`
3. **description**: Add `"A terminal UI for interacting with Agno AgentOS"` (per user decision)
4. **author**: Add author object:
   ```json
   "author": {
     "name": "Adam Shedivy",
     "url": "https://github.com/ajshedivy"
   }
   ```
5. **repository**: Add repository object:
   ```json
   "repository": {
     "type": "git",
     "url": "https://github.com/ajshedivy/openagent"
   }
   ```
6. **homepage**: Add `"https://github.com/ajshedivy/openagent#readme"`
7. **keywords**: Add npm discoverability tags:
   ```json
   "keywords": ["cli", "tui", "terminal", "ai", "agents", "agno", "agentos", "openagent"]
   ```

**Keep unchanged** (per user decisions):
- `"private": true` — Phase 16 will remove this
- `"bin": { "openagent": "./bin/openagent" }` — already correct
- `"license": "MIT"` — unchanged
- `"exports"` — deferred to publishing phase
- All `dependencies`, `devDependencies`, `scripts` — unchanged

**Field ordering**: Place identity fields near the top of the JSON object, after `$schema`. Suggested order: `$schema`, `name`, `version`, `description`, `author`, `homepage`, `repository`, `keywords`, `type`, `license`, `private`, `scripts`, `bin`, `exports`, then dependencies.
  </action>
  <verify>
Run these checks:
1. `cd /Users/adamshedivy/Documents/IBM/sandbox/oss/ai/opencode-agno && cat packages/opencode/package.json | node -e "const p=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('name:', p.name); console.log('version:', p.version); console.log('description:', p.description); console.log('author:', JSON.stringify(p.author)); console.log('repository:', JSON.stringify(p.repository)); console.log('homepage:', p.homepage); console.log('keywords:', JSON.stringify(p.keywords)); console.log('bin:', JSON.stringify(p.bin)); console.log('private:', p.private); console.log('license:', p.license);"` — verify all fields are correct
2. `cd /Users/adamshedivy/Documents/IBM/sandbox/oss/ai/opencode-agno && node -e "JSON.parse(require('fs').readFileSync('packages/opencode/package.json','utf8')); console.log('Valid JSON')"` — verify valid JSON
3. `cd /Users/adamshedivy/Documents/IBM/sandbox/oss/ai/opencode-agno/packages/opencode && bunx tsc --noEmit 2>&1 | head -5` — verify no type errors introduced
  </verify>
  <done>
- name is `@worksofadam/openagent`
- version is `0.1.0`
- description is "A terminal UI for interacting with Agno AgentOS"
- author has name "Adam Shedivy" with GitHub URL
- repository points to `https://github.com/ajshedivy/openagent`
- homepage is `https://github.com/ajshedivy/openagent#readme`
- keywords include relevant npm discoverability tags
- bin still maps `openagent` to `./bin/openagent`
- private is still true
- license is still MIT
- package.json is valid JSON
- TypeScript compilation still passes
  </done>
</task>

</tasks>

<verification>
1. `packages/opencode/package.json` is valid JSON
2. Package name is exactly `@worksofadam/openagent`
3. Package version is exactly `0.1.0`
4. Description references "openagent" and "AgentOS"
5. Bin field correctly maps `openagent` command to `./bin/openagent`
6. `private: true` is preserved (Phase 16 will remove it)
7. No existing functionality broken (typecheck passes)
</verification>

<success_criteria>
- PUB-03 satisfied: package.json sets name to `@worksofadam/openagent`, version `0.1.0`
- BRD-03 satisfied: Package display name in package.json reflects "openagent"
- All existing scripts, dependencies, and bin mapping unchanged
- Valid JSON, typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-package-configuration/12-01-SUMMARY.md`
</output>
