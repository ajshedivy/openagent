---
phase: 02-cli-and-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/bin/openagent
  - packages/opencode/package.json
autonomous: true

must_haves:
  truths:
    - "User can invoke `openagent` command after local install"
    - "Wrapper script errors mention openagent instead of opencode"
    - "Platform binary resolution targets openagent package names"
  artifacts:
    - path: "packages/opencode/bin/openagent"
      provides: "Node wrapper that resolves and runs platform binary"
      contains: "openagent-"
    - path: "packages/opencode/package.json"
      provides: "bin mapping for openagent command"
      contains: '"openagent": "./bin/openagent"'
  key_links:
    - from: "packages/opencode/package.json"
      to: "packages/opencode/bin/openagent"
      via: "bin field mapping"
      pattern: '"openagent": "./bin/openagent"'
    - from: "packages/opencode/bin/openagent"
      to: "node_modules/openagent-{platform}-{arch}/bin/openagent"
      via: "base/binary string construction"
      pattern: "openagent-"
---

<objective>
Rename the CLI binary to openagent and update the wrapper script so local installs resolve and run the new command name.

Purpose: Deliver the openagent command name across the bin entry and wrapper script.
Output: New bin script path and updated package.json bin mapping with openagent naming.
</objective>

<execution_context>
@/Users/adamshedivy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cli-and-branding/02-RESEARCH.md

@packages/opencode/bin/opencode
@packages/opencode/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename bin script and update bin mapping</name>
  <files>packages/opencode/bin/openagent, packages/opencode/package.json</files>
  <action>
Rename `packages/opencode/bin/opencode` to `packages/opencode/bin/openagent`. Update `packages/opencode/package.json` `bin` field to map `openagent` to `./bin/openagent` and remove the old `opencode` entry. Preserve the existing shebang and file contents during the rename.
  </action>
  <verify>`test -f packages/opencode/bin/openagent` and `grep -n '"openagent"' packages/opencode/package.json`</verify>
  <done>`bin.openagent` points to `./bin/openagent` and the old `opencode` bin entry is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update wrapper naming references</name>
  <files>packages/opencode/bin/openagent</files>
  <action>
In `packages/opencode/bin/openagent`, update the internal name strings to `openagent`:
- `base` should use `openagent-` prefix.
- `binary` should be `openagent` (and `openagent.exe` on Windows).
- Error message should mention the openagent CLI and the `openagent-{platform}-{arch}` package.
Do NOT rename the `OPENCODE_BIN_PATH` environment variable in this phase to preserve compatibility (rename is deferred to v2).
  </action>
  <verify>`grep -n "opencode" packages/opencode/bin/openagent` returns only the `OPENCODE_BIN_PATH` line.</verify>
  <done>Wrapper constructs openagent package/binary names and reports openagent in errors while keeping `OPENCODE_BIN_PATH` unchanged.</done>
</task>

</tasks>

<verification>
- `cd packages/opencode && npm unlink -g opencode || true && npm link`
- `which openagent` shows the global bin symlink and `openagent --version` runs without error.
</verification>

<success_criteria>

- Running `openagent` after a local link executes the CLI without falling back to `opencode` naming.
- No user-facing error message in the wrapper references `opencode`.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cli-and-branding/02-01-SUMMARY.md`
</output>
