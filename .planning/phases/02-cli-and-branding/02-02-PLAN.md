---
phase: 02-cli-and-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/cli/logo.ts
  - README.md
autonomous: true

must_haves:
  truths:
    - "ASCII art banner displays openagent in the TUI"
    - "README top section explains openagent initiative and AgentOS focus"
    - "Install instructions use the openagent command"
  artifacts:
    - path: "packages/opencode/src/cli/logo.ts"
      provides: "ASCII art arrays for openagent logo"
      contains: "openagent"
    - path: "README.md"
      provides: "Openagent initiative section and updated command examples"
      contains: "openagent"
  key_links:
    - from: "packages/opencode/src/cli/logo.ts"
      to: "packages/opencode/src/cli/cmd/tui/component/logo.tsx"
      via: "logo import"
      pattern: "import { logo"
    - from: "README.md"
      to: "CLI install commands"
      via: "code blocks"
      pattern: "openagent"
---

<objective>
Update CLI branding to show openagent in the ASCII logo and add an openagent initiative section at the top of the README with updated command examples.

Purpose: Ensure the TUI and documentation reflect the openagent identity.
Output: New ASCII logo text and updated README top section plus install command usage.
</objective>

<execution_context>
@/Users/adamshedivy/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cli-and-branding/02-RESEARCH.md

@packages/opencode/src/cli/logo.ts
@packages/opencode/src/cli/cmd/tui/component/logo.tsx
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ASCII art logo with openagent text</name>
  <files>packages/opencode/src/cli/logo.ts</files>
  <action>
Regenerate the ASCII art so the logo reads "openagent" while keeping the existing 3D/shadow style. Preserve the `left` and `right` arrays and the `marks` characters. Ensure each line has consistent width and the left/right arrays keep the same number of lines and alignment as before.
  </action>
  <verify>Run `bun run --conditions=browser ./src/index.ts` in `packages/opencode` and visually confirm the banner reads "openagent" without alignment issues.</verify>
  <done>TUI banner shows "openagent" and the shadow rendering still aligns with the markers.</done>
</task>

<task type="auto">
  <name>Task 2: Add openagent initiative section and update install commands</name>
  <files>README.md</files>
  <action>
Add an English-only "Openagent initiative" section near the top of `README.md` (after the badges and before Installation) describing the fork, the AgentOS-only focus, and the goal of a terminal-first client. Update command examples in the Installation section to use `openagent` instead of `opencode`, keeping URLs and package names unchanged unless they explicitly include the command invocation.
  </action>
  <verify>`grep -n "openagent" README.md` shows the new section and the installation commands reference `openagent`.</verify>
  <done>README top section explains the openagent initiative and install commands reference `openagent`.</done>
</task>

</tasks>

<verification>
- Visual check: TUI logo displays "openagent" with proper alignment.
- README top section introduces openagent initiative and AgentOS focus, and all install commands use `openagent`.
</verification>

<success_criteria>

- The CLI banner visibly says "openagent" in the TUI.
- README clearly distinguishes openagent and shows the new command in install steps.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-cli-and-branding/02-02-SUMMARY.md`
</output>
