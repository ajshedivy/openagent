---
phase: 15-github-actions-cicd-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish.yml
  - .github/workflows/deploy.yml
  - .github/workflows/opencode.yml
  - .github/workflows/publish-vscode.yml
  - .github/workflows/publish-github-action.yml
  - .github/workflows/release-github-action.yml
  - .github/workflows/sync-zed-extension.yml
  - .github/workflows/review.yml
  - .github/workflows/nix-desktop.yml
  - .github/workflows/update-nix-hashes.yml
  - .github/workflows/docs-update.yml
  - .github/workflows/stats.yml
  - .github/workflows/duplicate-issues.yml
  - .github/workflows/duplicate-prs.yml
  - .github/workflows/triage.yml
  - .github/workflows/notify-discord.yml
  - .github/workflows/generate.yml
  - .github/workflows/test.yml
  - .github/workflows/typecheck.yml
  - .github/workflows/pr-standards.yml
autonomous: true

must_haves:
  truths:
    - "17 opencode-specific workflows are disabled via `if: false` at the job level"
    - "No workflow files are deleted — all 24 files remain in .github/workflows/"
    - "test.yml push trigger targets `main` branch instead of `dev`"
    - "typecheck.yml PR trigger targets `main` branch instead of `dev`"
    - "pr-standards.yml team whitelist contains fork owner instead of opencode team"
    - "5 maintenance workflows (stale-issues, close-stale-prs, contributors-label, daily-issues-recap, daily-pr-recap) are untouched"
  artifacts:
    - path: ".github/workflows/publish.yml"
      provides: "Disabled publish workflow"
      contains: "if: false"
    - path: ".github/workflows/test.yml"
      provides: "Test workflow targeting main branch"
      contains: "main"
    - path: ".github/workflows/typecheck.yml"
      provides: "Typecheck workflow targeting main branch"
      contains: "main"
    - path: ".github/workflows/pr-standards.yml"
      provides: "PR standards with fork owner whitelist"
      contains: "ajshedivy"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "CI pipeline"
      via: "push to main triggers tests"
      pattern: "branches: main"
    - from: ".github/workflows/typecheck.yml"
      to: "CI pipeline"
      via: "PRs to main trigger typecheck"
      pattern: "branches: [main]"
---

<objective>
Disable 17 irrelevant opencode-specific GitHub Actions workflows by adding `if: false` at the job level, and update 3 essential CI workflows (test, typecheck, pr-standards) to work correctly for the openagent fork.

Purpose: Prevent fork from running upstream workflows that depend on opencode-specific secrets, repos, and infrastructure. Ensure essential CI (tests, typecheck, PR checks) runs correctly on the `main` branch.

Output: 20 workflow files modified — 17 disabled, 3 updated. 4 workflows remain untouched. No files deleted.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable opencode-specific publish and deploy workflows</name>
  <files>.github/workflows/publish.yml, .github/workflows/deploy.yml, .github/workflows/publish-vscode.yml, .github/workflows/publish-github-action.yml, .github/workflows/release-github-action.yml, .github/workflows/sync-zed-extension.yml, .github/workflows/notify-discord.yml</files>
  <action>
Add `if: false` to every job in these 7 publishing/deployment workflows. This disables them without removing the files.

**publish.yml** — 3 jobs to disable:
- Job `publish` (line 32): Already has `if: github.repository == 'anomalyco/opencode'`. Replace with `if: false`
- Job `publish-tauri` (line 93): Add `if: false` after `needs: publish` (line 94)
- Job `publish-release` (line 208): Add `if: false` after the `needs:` block (after line 211)

**deploy.yml** — 1 job to disable:
- Job `deploy` (line 13): Add `if: false` after `runs-on:` line 14

**publish-vscode.yml** — 1 job to disable:
- Job `publish` (line 15): Add `if: false` after `runs-on:` line 16

**publish-github-action.yml** — 1 job to disable:
- Job `publish` (line 15): Add `if: false` after `runs-on:` line 17

**release-github-action.yml** — 1 job to disable:
- Job `release` (line 15): Add `if: false` after `runs-on:` line 18

**sync-zed-extension.yml** — 1 job to disable:
- Job `zed` (line 9): Add `if: false` after `runs-on:` line 11

**notify-discord.yml** — 1 job to disable:
- Job `notify` (line 8): Add `if: false` after `runs-on:` line 9

For each job, add the `if: false` line directly under the job name, before or after `runs-on`. The exact placement should be:
```yaml
jobs:
  job-name:
    if: false
    runs-on: ...
```
  </action>
  <verify>
Verify `if: false` appears in every job of these 7 files:
```bash
for f in publish.yml deploy.yml publish-vscode.yml publish-github-action.yml release-github-action.yml sync-zed-extension.yml notify-discord.yml; do
  echo "=== $f ==="
  grep -c "if: false" .github/workflows/$f
done
```

Expected counts: publish.yml=3, deploy.yml=1, publish-vscode.yml=1, publish-github-action.yml=1, release-github-action.yml=1, sync-zed-extension.yml=1, notify-discord.yml=1.
  </verify>
  <done>
- All 7 publish/deploy workflows disabled via `if: false` on every job
- No workflow files deleted
- Total of 9 jobs disabled across 7 files
  </done>
</task>

<task type="auto">
  <name>Task 2: Disable opencode AI-powered workflows</name>
  <files>.github/workflows/opencode.yml, .github/workflows/review.yml, .github/workflows/docs-update.yml, .github/workflows/duplicate-issues.yml, .github/workflows/duplicate-prs.yml, .github/workflows/triage.yml</files>
  <action>
Add `if: false` to every job in these 6 AI-powered workflows that use opencode-specific API keys and services.

**opencode.yml** — 1 job to disable:
- Job `opencode` (line 10): Already has a multi-line `if:` condition (lines 11-15). Replace the entire `if:` block with `if: false`

**review.yml** — 1 job to disable:
- Job `check-guidelines` (line 8): Already has a multi-line `if:` condition (lines 9-12). Replace the entire `if:` block with `if: false`

**docs-update.yml** — 1 job to disable:
- Job `update-docs` (line 12): Already has `if: github.repository == 'sst/opencode'`. Replace with `if: false`

**duplicate-issues.yml** — 1 job to disable:
- Job `check-duplicates` (line 8): Add `if: false` after `runs-on:` line 9

**duplicate-prs.yml** — 1 job to disable:
- Job `check-duplicates` (line 8): Already has a multi-line `if:` condition (lines 9-19). Replace the entire `if:` block with `if: false`

**triage.yml** — 1 job to disable:
- Job `triage` (line 8): Add `if: false` after `runs-on:` line 9

For jobs that already have `if:` conditions, replace the existing condition with just `if: false`. For jobs without `if:`, add `if: false` directly under the job key.
  </action>
  <verify>
Verify `if: false` appears in all 6 files:
```bash
for f in opencode.yml review.yml docs-update.yml duplicate-issues.yml duplicate-prs.yml triage.yml; do
  echo "=== $f ==="
  grep -c "if: false" .github/workflows/$f
done
```

Expected: Each file has exactly 1 match.

Also verify the old multi-line conditions are removed:
```bash
grep -l "anomalyco/opencode\|sst/opencode\|/oc\|/review\|/opencode" .github/workflows/opencode.yml .github/workflows/review.yml .github/workflows/docs-update.yml .github/workflows/duplicate-prs.yml 2>/dev/null || echo "All old conditions removed"
```
  </verify>
  <done>
- All 6 AI-powered workflows disabled via `if: false`
- Multi-line `if:` conditions replaced with simple `if: false`
- No workflow files deleted
  </done>
</task>

<task type="auto">
  <name>Task 3: Disable Nix, stats, and generate workflows</name>
  <files>.github/workflows/nix-desktop.yml, .github/workflows/update-nix-hashes.yml, .github/workflows/stats.yml, .github/workflows/generate.yml</files>
  <action>
Add `if: false` to every job in these 4 infrastructure/utility workflows.

**nix-desktop.yml** — 1 job to disable:
- Job `build-desktop` (line 23): Add `if: false` before `strategy:` (line 24)

**update-nix-hashes.yml** — 1 job to disable:
- Job `update-node-modules-hashes` (line 24): Already has `if:` condition (line 25). Replace with `if: false`

**stats.yml** — 1 job to disable:
- Job `stats` (line 11): Already has `if: github.repository == 'anomalyco/opencode'`. Replace with `if: false`

**generate.yml** — 1 job to disable:
- Job `generate` (line 10): Add `if: false` after `runs-on:` line 11
  </action>
  <verify>
Verify `if: false` in all 4 files:
```bash
for f in nix-desktop.yml update-nix-hashes.yml stats.yml generate.yml; do
  echo "=== $f ==="
  grep -c "if: false" .github/workflows/$f
done
```

Expected: Each file has exactly 1 match.
  </verify>
  <done>
- All 4 infrastructure/utility workflows disabled via `if: false`
- Existing `if:` conditions replaced with `if: false`
- No workflow files deleted
  </done>
</task>

<task type="auto">
  <name>Task 4: Update test.yml triggers for main branch</name>
  <files>.github/workflows/test.yml</files>
  <action>
Update the push trigger branch from `dev` to `main`. Keep the `pull_request` trigger unchanged (it has no branch filter, which means it triggers on all PRs — this is correct).

**Change** (lines 4-7):
```yaml
on:
  push:
    branches:
      - dev
```

**To:**
```yaml
on:
  push:
    branches:
      - main
```

Keep `pull_request` and `workflow_dispatch` triggers unchanged.
  </action>
  <verify>
Verify the push trigger targets main:
```bash
grep -A 3 "push:" .github/workflows/test.yml | head -4
```

Expected output:
```
  push:
    branches:
      - main
```

Verify pull_request trigger is unchanged:
```bash
grep "pull_request" .github/workflows/test.yml
```

Expected: `  pull_request:` (no branch filter)
  </verify>
  <done>
- test.yml push trigger changed from `dev` to `main`
- pull_request trigger preserved (triggers on all PRs)
- workflow_dispatch trigger preserved
  </done>
</task>

<task type="auto">
  <name>Task 5: Update typecheck.yml triggers for main branch</name>
  <files>.github/workflows/typecheck.yml</files>
  <action>
Update the pull_request trigger from PRs to `dev` to PRs to `main`.

**Change** (lines 4-5):
```yaml
on:
  pull_request:
    branches: [dev]
```

**To:**
```yaml
on:
  pull_request:
    branches: [main]
```

Keep `workflow_dispatch` trigger unchanged.
  </action>
  <verify>
Verify the PR trigger targets main:
```bash
grep -A 2 "pull_request:" .github/workflows/typecheck.yml
```

Expected output includes `branches: [main]`.
  </verify>
  <done>
- typecheck.yml PR trigger changed from `dev` to `main`
- workflow_dispatch trigger preserved
  </done>
</task>

<task type="auto">
  <name>Task 6: Update pr-standards.yml team whitelist</name>
  <files>.github/workflows/pr-standards.yml</files>
  <action>
Replace the opencode team member whitelist with the fork owner. The current whitelist (lines 9-19) excludes specific opencode team members from PR standards checks. Update to exclude the fork owner instead.

**Change** the multi-line `if:` condition (lines 9-19):
```yaml
    if: |
      github.event.pull_request.user.login != 'actions-user' &&
      github.event.pull_request.user.login != 'opencode' &&
      github.event.pull_request.user.login != 'rekram1-node' &&
      github.event.pull_request.user.login != 'thdxr' &&
      github.event.pull_request.user.login != 'kommander' &&
      github.event.pull_request.user.login != 'jayair' &&
      github.event.pull_request.user.login != 'fwang' &&
      github.event.pull_request.user.login != 'adamdotdevin' &&
      github.event.pull_request.user.login != 'iamdavidhill' &&
      github.event.pull_request.user.login != 'opencode-agent[bot]'
```

**To:**
```yaml
    if: |
      github.event.pull_request.user.login != 'actions-user' &&
      github.event.pull_request.user.login != 'ajshedivy'
```

This keeps the `actions-user` exclusion (for automated commits) and adds only the fork owner.
  </action>
  <verify>
Verify the updated whitelist:
```bash
grep -A 4 "if: |" .github/workflows/pr-standards.yml
```

Expected: Only `actions-user` and `ajshedivy` in the whitelist.

Verify old team members are removed:
```bash
grep -c "thdxr\|kommander\|jayair\|fwang\|rekram1-node\|adamdotdevin\|iamdavidhill\|opencode-agent" .github/workflows/pr-standards.yml
```

Expected: 0
  </verify>
  <done>
- PR standards whitelist updated to fork owner (ajshedivy)
- Old opencode team members removed from whitelist
- actions-user exclusion preserved for automated commits
  </done>
</task>

</tasks>

<verification>
**Post-execution checks:**

1. **Count all disabled workflows (17 total):**
   ```bash
   count=0
   for f in publish.yml deploy.yml opencode.yml publish-vscode.yml publish-github-action.yml release-github-action.yml sync-zed-extension.yml review.yml nix-desktop.yml update-nix-hashes.yml docs-update.yml stats.yml duplicate-issues.yml duplicate-prs.yml triage.yml notify-discord.yml generate.yml; do
     if grep -q "if: false" .github/workflows/$f; then
       count=$((count + 1))
     fi
   done
   echo "Disabled workflows: $count/17"
   ```
   Expected: 17/17

2. **Verify all 24 workflow files still exist:**
   ```bash
   ls .github/workflows/*.yml | wc -l
   ```
   Expected: 24 (original count, plus potentially the new publish workflow from Phase 16)

3. **Verify untouched workflows are truly unchanged:**
   ```bash
   git diff .github/workflows/stale-issues.yml .github/workflows/close-stale-prs.yml .github/workflows/contributors-label.yml .github/workflows/daily-issues-recap.yml .github/workflows/daily-pr-recap.yml
   ```
   Expected: No diff output (5 files unchanged)

4. **Verify test.yml targets main:**
   ```bash
   grep -A 2 "push:" .github/workflows/test.yml | grep "main"
   ```
   Expected: `- main`

5. **Verify typecheck.yml targets main:**
   ```bash
   grep "branches:" .github/workflows/typecheck.yml
   ```
   Expected: `branches: [main]`

6. **Verify pr-standards.yml has correct whitelist:**
   ```bash
   grep "ajshedivy" .github/workflows/pr-standards.yml
   ```
   Expected: Match found

7. **No opencode-specific repo checks remain active:**
   ```bash
   # Check that disabled files don't have active repo checks
   for f in publish.yml stats.yml docs-update.yml; do
     active=$(grep "if:" .github/workflows/$f | grep -v "false" | grep -c "anomalyco\|sst/opencode" || true)
     echo "$f active repo checks: $active"
   done
   ```
   Expected: All 0 (repo checks replaced by `if: false`)
</verification>

<success_criteria>
1. 17 opencode-specific workflows disabled via `if: false` at every job level
2. All 24 workflow files remain in .github/workflows/ (none deleted)
3. test.yml push trigger targets `main` instead of `dev`
4. typecheck.yml PR trigger targets `main` instead of `dev`
5. pr-standards.yml whitelist contains `ajshedivy` instead of opencode team
6. 5 maintenance workflows (stale-issues, close-stale-prs, contributors-label, daily-issues-recap, daily-pr-recap) are completely untouched
7. No active opencode-specific repo checks (anomalyco/opencode, sst/opencode) remain in any workflow
</success_criteria>

<output>
After completion, create `.planning/phases/15-github-actions-cicd-cleanup/15-01-SUMMARY.md`
</output>
