---
phase: 03-ui-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/cli/cmd/tui/component/dialog-agno.tsx
autonomous: true

must_haves:
  truths:
    - "AgentOS Hub dialog displays three tabs: Agents, Teams, Workflows"
    - "Active tab has colored underline indicator using theme accent color"
    - "Tab key cycles through tabs: Agents -> Teams -> Workflows -> Agents"
    - "Search field is visible at top of dialog"
    - "Keyboard hints (Tab, Enter, Esc) are displayed at bottom"
  artifacts:
    - path: "packages/opencode/src/cli/cmd/tui/component/dialog-agno.tsx"
      provides: "Tabbed dialog hub component for AgentOS"
      exports: ["DialogAgno"]
      min_lines: 80
  key_links:
    - from: "dialog-agno.tsx"
      to: "@tui/ui/dialog"
      via: "useDialog import"
      pattern: "useDialog"
    - from: "dialog-agno.tsx"
      to: "@tui/context/theme"
      via: "useTheme import"
      pattern: "useTheme"
---

<objective>
Create the DialogAgno tabbed hub component that displays Agents | Teams | Workflows tabs with underline-style navigation and keyboard controls.

Purpose: Establish the UI infrastructure for the AgentOS hub - the shell and navigation that later phases will populate with content.

Output: A new `dialog-agno.tsx` component that renders a tabbed dialog matching existing opencode dialog patterns.
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-infrastructure/03-CONTEXT.md

# Reference patterns - read these for implementation guidance:
@packages/opencode/src/cli/cmd/tui/ui/dialog.tsx
@packages/opencode/src/cli/cmd/tui/ui/dialog-select.tsx
@packages/opencode/src/cli/cmd/tui/ui/dialog-export-options.tsx
@packages/opencode/src/cli/cmd/tui/component/dialog-model.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DialogAgno tabbed hub component</name>
  <files>packages/opencode/src/cli/cmd/tui/component/dialog-agno.tsx</files>
  <action>
Create a new DialogAgno component in the TUI component directory. Follow these implementation requirements from the phase context (locked decisions):

**Tab bar design:**
- Underline style: active tab has colored underline beneath text
- Horizontal tabs at top of dialog, content area below
- Text-only labels: "Agents", "Teams", "Workflows"
- Use theme.primary/accent color for active tab underline

**Dialog layout:**
- Use useDialog() hook for dialog context
- Use useTheme() for consistent theming
- Title: "AgentOS Hub" displayed in header (follow DialogSelect pattern for header layout with title and "esc" hint)
- Content-aware sizing matching "Select model" dialog patterns

**State management:**
- Use createStore from solid-js/store for tab state
- Track active tab: "agents" | "teams" | "workflows"
- Default to "agents" tab on open

**Keyboard navigation:**
- Use useKeyboard from @opentui/solid
- Tab key cycles: Agents -> Teams -> Workflows -> Agents
- Prevent default on Tab key to avoid losing focus

**Search field:**
- Add input field at top (below tabs)
- Placeholder: "Search"
- Wire to filter state (actual filtering happens in later phases)

**Keyboard hints:**
- Display at bottom: "Tab" for switching sections, "Enter" to select, "Esc" to close
- Follow DialogSelect pattern for hint styling (theme.textMuted)

**Content areas (placeholders for now):**
- When "agents" tab active: show placeholder text "Loading agents..."
- When "teams" tab active: show placeholder text "Coming soon"
- When "workflows" tab active: show placeholder text "Coming soon"

Import pattern to follow:
```typescript
import { useKeyboard } from "@opentui/solid"
import { createStore } from "solid-js/store"
import { useTheme } from "@tui/context/theme"
import { useDialog } from "@tui/ui/dialog"
import { TextAttributes } from "@opentui/core"
import { Show, For, onMount } from "solid-js"
```

Export the component as a named export: `export function DialogAgno()`

Also export a static show method following DialogSelect pattern:
```typescript
DialogAgno.show = (dialog: DialogContext) => {
  dialog.replace(() => <DialogAgno />)
}
```
  </action>
  <verify>
1. File exists at packages/opencode/src/cli/cmd/tui/component/dialog-agno.tsx
2. Run: `cd /Users/adamshedivy/Documents/IBM/sandbox/oss/ai/opencode-agno && bun run check` - TypeScript compiles without errors
3. Component exports DialogAgno function
4. Component imports useDialog, useTheme, useKeyboard, createStore
  </verify>
  <done>
DialogAgno component exists with:
- Three tabs (Agents, Teams, Workflows) with underline-style active indicator
- Tab key cycles between sections
- Search input at top
- Keyboard hints at bottom
- Placeholder content for each section
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. TypeScript compilation: `bun run check` passes
2. File structure: dialog-agno.tsx exists in component directory
3. Exports: DialogAgno is exported and can be imported
</verification>

<success_criteria>
- DialogAgno component created with tabbed UI matching design decisions
- Three tabs visible: Agents, Teams, Workflows
- Tab key cycles through tabs with visual underline indicator
- Search field visible at top
- Keyboard hints visible at bottom
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-infrastructure/03-01-SUMMARY.md`
</output>
