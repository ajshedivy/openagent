---
phase: 03-ui-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/opencode/src/cli/cmd/tui/app.tsx
autonomous: true

must_haves:
  truths:
    - "User can type /agno in chat to open the AgentOS Hub dialog"
    - "The /agno command appears in the command list dialog"
    - "Selecting /agno or typing it opens DialogAgno"
  artifacts:
    - path: "packages/opencode/src/cli/cmd/tui/app.tsx"
      provides: "Command registration for /agno slash command"
      contains: "slash.*agno"
  key_links:
    - from: "app.tsx"
      to: "dialog-agno.tsx"
      via: "import and dialog.replace()"
      pattern: "DialogAgno"
---

<objective>
Register the `/agno` slash command in the TUI command system so users can open the AgentOS Hub.

Purpose: Make the hub accessible via the standard slash command interface that opencode users are familiar with.

Output: Updated app.tsx with the /agno command registered in command.register().
</objective>

<execution_context>
@/Users/adamshedivy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamshedivy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-infrastructure/03-CONTEXT.md
@.planning/phases/03-ui-infrastructure/03-01-SUMMARY.md

# Reference for command registration pattern:
@packages/opencode/src/cli/cmd/tui/app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import DialogAgno in app.tsx</name>
  <files>packages/opencode/src/cli/cmd/tui/app.tsx</files>
  <action>
Add import for DialogAgno at the top of app.tsx with the other dialog imports:

```typescript
import { DialogAgno } from "@tui/component/dialog-agno"
```

Place this import near the other dialog imports (around line 14-21 where DialogModel, DialogMcp, DialogStatus, DialogAgent are imported).
  </action>
  <verify>
1. Import statement exists in app.tsx
2. `bun run check` passes (no TypeScript errors)
  </verify>
  <done>
DialogAgno is imported in app.tsx without TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register /agno slash command</name>
  <files>packages/opencode/src/cli/cmd/tui/app.tsx</files>
  <action>
Add the /agno command registration inside the command.register() callback (around line 284-583). Insert a new command option following the existing pattern.

Add this command in the "Agent" category section (near other agent-related commands like "Switch agent"):

```typescript
{
  title: "AgentOS Hub",
  value: "agno.hub",
  category: "Agent",
  slash: {
    name: "agno",
    aliases: ["hub", "agentos"],
  },
  onSelect: () => {
    dialog.replace(() => <DialogAgno />)
  },
},
```

Place this after the "Switch agent" command (around line 382-384) to keep agent-related commands grouped together.

The command includes:
- title: "AgentOS Hub" (displays in command list)
- value: "agno.hub" (unique identifier)
- category: "Agent" (groups with other agent commands)
- slash.name: "agno" (the primary slash command)
- slash.aliases: ["hub", "agentos"] (alternative ways to invoke)
- onSelect: Opens DialogAgno via dialog.replace()
  </action>
  <verify>
1. Command registration exists in app.tsx
2. `bun run check` passes
3. grep -n "agno.hub" packages/opencode/src/cli/cmd/tui/app.tsx returns a match
  </verify>
  <done>
The /agno command is registered with:
- Title "AgentOS Hub" in command list
- Slash command /agno (and aliases /hub, /agentos)
- Opens DialogAgno when selected
- Grouped in "Agent" category
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. TypeScript compilation: `bun run check` passes
2. Import present: grep for "DialogAgno" in app.tsx
3. Command registered: grep for "agno.hub" in app.tsx
4. Slash command works: The /agno command should appear in slash suggestions
</verification>

<success_criteria>
- DialogAgno imported in app.tsx
- /agno slash command registered with aliases /hub and /agentos
- Command appears in Agent category
- TypeScript compiles without errors
- Selecting the command opens the AgentOS Hub dialog
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-infrastructure/03-02-SUMMARY.md`
</output>
