# Milestone v3.0: Deployment & Branding

**Status:** SHIPPED 2026-02-09
**Phases:** 12-18 (Phase 17 dropped)
**Total Plans:** 7

## Overview

Made openagent installable, fully branded, and config-compatible. Users can clone the repo, run `install.sh`, and get a working `openagent` binary. npm publishing pipeline prepared but deferred; local install script provides immediate installability.

## Phases

### Phase 12: Package Configuration

**Goal**: Package metadata reflects openagent brand and scoped publishing name
**Depends on**: Nothing (first phase of milestone)
**Plans**: 1 plan

Plans:
- [x] 12-01-PLAN.md — Update package.json identity fields (name, version, description, author, repository, keywords)

**Details:**
Established package identity as @worksofadam/openagent v0.1.0. Updated name, version, description, author, repository URL, homepage, bugs URL, and keywords in package.json.

### Phase 13: Config File Dual Support

**Goal**: Users can configure openagent using either openagent.json or opencode.json with identical behavior
**Depends on**: Phase 12
**Plans**: 1 plan

Plans:
- [x] 13-01-PLAN.md — Add dual config file resolution (openagent.json preferred, opencode.json fallback)

**Details:**
App reads openagent.json as primary config, falls back to opencode.json. Existing OPENCODE_* env vars continue working. Both config file formats validate identically.

### Phase 14: User-Facing Text Branding

**Goal**: All user-visible text references "openagent" instead of "opencode"
**Depends on**: Phase 13
**Plans**: 2 plans

Plans:
- [x] 14-01-PLAN.md — Update CLI scriptName, command descriptions, and console messages to reference "openagent"
- [x] 14-02-PLAN.md — Gap closure: Update TUI tips, notifications, and provider dialogs to reference "openagent"

**Details:**
Comprehensive branding pass across CLI help text, TUI tips, notifications, provider dialogs, error messages, and log output. All user-visible text consistently uses lowercase "openagent". Internal identifiers (provider IDs, theme names) remain "opencode" as they're not user-facing.

### Phase 15: GitHub Actions CI/CD Cleanup

**Goal**: Disable irrelevant opencode-specific workflows and configure essential CI workflows for the openagent fork
**Depends on**: Phase 14
**Plans**: 1 plan

Plans:
- [x] 15-01-PLAN.md — Disable 17 opencode-specific workflows and update test/typecheck/pr-standards for fork

**Details:**
Disabled 17 upstream workflows via `if: false` (no files deleted). Updated 3 essential CI workflows: test and typecheck trigger on PRs to main, PR standards workflow has correct team whitelist.

### Phase 16: Publishing Pipeline

**Goal**: GitHub Actions workflow builds and publishes @worksofadam/openagent to npm on release tags
**Depends on**: Phase 15
**Plans**: 1 plan

Plans:
- [x] 16-01-PLAN.md — Create publish-openagent.yml (npm publish on v* tags), ci.yml (PR quality gate), and fix package.json for publishing

**Details:**
Created publish-openagent.yml for npm publishing on v* tags, ci.yml for PR quality gates (typecheck + build). Removed private flag from package.json, added files field to control publish contents. npm publishing itself deferred to future milestone.

### Phase 17: Release Verification (DROPPED)

**Goal**: Package is published, tagged, and verified installable end-to-end
**Status**: Dropped from v3.0 scope — npm publishing deferred in favor of local install script

### Phase 18: Local Install Script

**Goal**: Users can clone the repo and run `install.sh` to build openagent with platform-specific Bun-compiled binaries
**Depends on**: Phase 16
**Plans**: 1 plan

Plans:
- [x] 18-01-PLAN.md — Create install.sh (build-from-source script using bun build --compile, install to ~/.openagent/bin/)

**Details:**
Created install.sh build-from-source script. Compiles openagent for current platform via existing build.ts --single flag, installs to ~/.openagent/bin/ with PATH instructions. Handles missing Bun dependency with clear error messages.

---

## Milestone Summary

**Key Decisions:**

- Decouple GSD milestone from npm version (v3.0 milestone, v0.1.0 npm package)
- Scoped npm package only (@worksofadam/openagent)
- Dual config file support (openagent.json preferred, opencode.json fallback)
- Preserve OPENCODE_* env vars (backward compat)
- User-facing text consistently uses lowercase "openagent"
- Internal identifiers remain "opencode" (not user-facing)
- Pivot from npm publishing to local install script for v0.1.0
- Build from source using existing build.ts --single flag

**Issues Resolved:**

- Fixed packages/web/package.json workspace dependency to reference @worksofadam/openagent
- MCP auth command in status dialog updated to correct branding

**Issues Deferred:**

- npm publishing (Phase 17 dropped — pipeline ready but publish deferred)
- PUB-04: User can `npm i -g @worksofadam/openagent` — deferred
- REL-02: README npm install instructions — deferred (README has clone+install.sh instead)
- Platform-specific binary packages

**Technical Debt Incurred:**

- 14 files in build infrastructure still reference old bin/opencode path
- npm publishing workflow created but not tested end-to-end
- Phase 17 success criteria unmet (npm installability)

---

_For current project status, see .planning/ROADMAP.md_
